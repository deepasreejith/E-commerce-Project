<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta3/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css"
    />
    <link rel="stylesheet" href="style.css" />
    <link rel="stylesheet" href="cartstyle.css" />
    <link rel="stylesheet" href="productstyle.css" />
    <title>Beauty Bonanza</title>
    <link
      rel="shortcut icon"
      type="image"
      href="https://png.pngtree.com/png-clipart/20211024/original/pngtree-salon-logo-png-image_6872052.png"
      width="150px"
      height="150px"
    />
  </head>
  <body>
    <header>
      <nav
        style="background-color: rgb(249, 217, 244); height: 100px"
        class="navbar navbar-expand-lg"
      >
        <div class="container-fluid">
          <a class="navbar-brand" style="color: black" href="#"
            ><strong>Beauty Bonanza</strong></a
          >
          <button
            class="navbar-toggler"
            type="button"
            data-bs-toggle="collapse"
            data-bs-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent"
            aria-expanded="false"
            aria-label="Toggle navigation"
          >
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
              <li class="nav-item">
                <a
                  class="nav-link active"
                  style="color: rgb(0, 0, 0)"
                  aria-current="page"
                  href="/home"
                  >Home</a
                >
              </li>

              <li class="nav-item dropdown">
                <a
                  class="nav-link dropdown-toggle"
                  style="color: rgb(0, 0, 0)"
                  href="#"
                  id="navbarDropdown"
                  role="button"
                  data-bs-toggle="dropdown"
                  aria-expanded="false"
                >
                  Categories
                </a>
                <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                  <% category.forEach(function(category) { %>
                  <li>
                    <a href="/shop?id=<%= category._id %>" class="dropdown-item"
                      ><%= category.name %></a
                    >
                  </li>
                  <% }); %>
                </ul>
              </li>
              <li class="nav-item">
                <form
                  class="d-flex"
                  id="searchForm"
                  action="/search"
                  method="post"
                >
                  <input
                    class="form-control me-1"
                    name="name"
                    type="search"
                    placeholder="Search"
                    aria-label="Search"
                    style="width: 500px; border-radius: 50px"
                    value=""
                  />
                  <button
                    class="btn btn-outline-secondary"
                    type="submit"
                    style="border-radius: 10"
                  >
                    <i class="fas fa-search"></i>
                  </button>
                </form>
              </li>
            </ul>
            <div
              class="btn-group"
              style="margin-right: 25px; border-radius: 10"
            >
              <button
                class="btn btn-light btn-sm dropdown-toggle"
                type="button"
                data-bs-toggle="dropdown"
                aria-expanded="false"
              >
                <% if(typeof user !== 'undefined' && user) { %> <%= user.name %>
                <% } else { %> Account <% } %>
              </button>
              <ul class="dropdown-menu" style="z-index: 1; border-radius: 15">
                <% if (typeof user !== 'undefined' && user) { %>
                <li>
                  <a class="dropdown-item" href="/myaccount">My Account</a>
                </li>

                <li><a class="dropdown-item" href="/logout">Logout</a></li>

                <% } else { %>
                <li><a class="dropdown-item" href="/login">login</a></li>
                <% } %>
              </ul>

              <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                  <a class="nav-link active" aria-current="page" href="/cart"
                    ><i
                      class="fa-solid fa-cart-shopping"
                      style="color: #000000"
                    ></i>
                    <span class="badge bg-danger"><%= count %></span>
                  </a>
                </li>
              </ul>
            </div>
          </div>
        </div>
      </nav>
    </header>
  </body>
</html>
